# 作业相似性检测报告

- 作业：**第8次实验 — 算法与实现（演示 #8）**
- 语言：**python**
- 生成时间：**2026-04-27 20:13:57**

## 概览
- 总比对数：**378**
- 有效评分数：**378**
- 平均相似度：**98.40**
- 最高相似度：**100**
- 最低相似度：**40**
- 高风险阈值：**80**
- 高风险对数量：**370**

## 高风险对 Top 20
| 提交A | 提交B | 相似度 | 过滤层 | 原因(摘要) |
|---:|---:|---:|---|---|
| 198 | 199 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都实现了计算从1到n的整数和的功能，仅函数名和默认参数值不同，但这些是表面差异。 [向量相似度: 0.00] |
| 198 | 200 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都实现了计算从1到n的整数和的功能。唯一区别是函数名和默认参数值，但这些是表面差异，不影响核心算法。 [向量相似度: 0.00] |
| 198 | 201 | 100 | hybrid_pipeline | 两段代码在核心逻辑上完全相同，都实现了计算1到n整数之和的功能，逻辑流程、数据结构和关键算法均一致。 [向量相似度: 0.00] |
| 198 | 202 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是通过循环累加从1到n的整数（x**1等同于x），仅函数名和默认参数值不同，但这些是表面差异，不影响核心功能。 [向量相似度: 0.00] |
| 198 | 203 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都实现了计算从1到n的整数和的功能，仅函数名和默认值不同，但这些是表面差异。 [向量相似度: 0.00] |
| 198 | 204 | 100 | hybrid_pipeline | 两段代码在核心功能、逻辑流程、数据结构和关键算法上完全相同，都是计算从1到n的整数和。函数名和默认参数值不同，但这些是表面差异，不影响逻辑等价性。 [向量相似度: 0.00] |
| 198 | 205 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都实现了计算从1到n的整数和的功能，仅函数名和默认参数值不同，但这些是表面差异。 [向量相似度: 0.00] |
| 198 | 206 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是通过循环累加从1到n的整数（x**1等价于x）来计算总和。唯一的区别是函数名和默认参数值（49 vs 52），但这些是表面差异，不影响核心功能。 [向量相似度: 0.00] |
| 198 | 207 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是通过循环累加从1到n的整数（x**1等价于x）来计算总和。唯一的区别是函数名和默认参数值（49 vs 23），但这些是表面差异，不影响核心功能实现。 [向量相似度: 0.00] |
| 198 | 208 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是通过循环累加从1到n的整数之和，仅函数名和默认参数值不同，但这些是表面差异。 [向量相似度: 0.00] |
| 198 | 209 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是计算从1到n的整数累加和。仅函数名和默认参数值不同，但这些是表面差异，不影响核心功能。 [向量相似度: 0.00] |
| 198 | 210 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是通过循环累加从1到n的整数（x**1等价于x）来计算总和。唯一的区别是函数名和默认参数值（49 vs 55），但这些是表面差异，不影响核心功能。 [向量相似度: 0.00] |
| 198 | 211 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和算法意图上完全相同，都是通过循环累加从1到n的整数（x**1等价于x）来计算总和。函数名和默认参数值不同属于表面差异，不影响核心功能实现。 [向量相似度: 0.00] |
| 198 | 212 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是计算从1到n的整数和。唯一的区别是函数名和默认参数值，但这些是表面差异，不影响核心功能。 [向量相似度: 0.00] |
| 198 | 213 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是计算从1到n的整数之和。唯一的区别是函数名和默认参数值，但这些是表面差异，不影响核心功能。 [向量相似度: 0.00] |
| 198 | 214 | 100 | hybrid_pipeline | 两段代码都实现了从1到n的整数求和算法，逻辑流程、数据结构和关键算法完全相同，仅函数名和默认参数值不同，但这些是表面差异。 [向量相似度: 0.00] |
| 198 | 215 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是通过循环累加从1到n的整数之和。唯一区别是函数名和默认参数值，但这些不影响核心算法逻辑。 [向量相似度: 0.00] |
| 198 | 216 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是通过循环遍历从1到n的整数，累加每个整数的1次方（即整数本身），最终返回累加和。仅函数名和默认值不同，这些是表面差异，不影响核心逻辑。 [向量相似度: 0.00] |
| 198 | 217 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都实现了计算从1到n的整数和的功能。差异仅在于函数名和默认参数值，这些是表面差异，不影响核心逻辑。 [向量相似度: 0.00] |
| 198 | 218 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都实现了计算1到n整数累加和的功能，仅函数名和默认值不同，但这些是表面差异。 [向量相似度: 0.00] |

## 全部结果 Top 20
| 提交A | 提交B | 相似度 | 过滤层 | 原因(摘要) |
|---:|---:|---:|---|---|
| 198 | 199 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都实现了计算从1到n的整数和的功能，仅函数名和默认参数值不同，但这些是表面差异。 [向量相似度: 0.00] |
| 198 | 200 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都实现了计算从1到n的整数和的功能。唯一区别是函数名和默认参数值，但这些是表面差异，不影响核心算法。 [向量相似度: 0.00] |
| 198 | 201 | 100 | hybrid_pipeline | 两段代码在核心逻辑上完全相同，都实现了计算1到n整数之和的功能，逻辑流程、数据结构和关键算法均一致。 [向量相似度: 0.00] |
| 198 | 202 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是通过循环累加从1到n的整数（x**1等同于x），仅函数名和默认参数值不同，但这些是表面差异，不影响核心功能。 [向量相似度: 0.00] |
| 198 | 203 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都实现了计算从1到n的整数和的功能，仅函数名和默认值不同，但这些是表面差异。 [向量相似度: 0.00] |
| 198 | 204 | 100 | hybrid_pipeline | 两段代码在核心功能、逻辑流程、数据结构和关键算法上完全相同，都是计算从1到n的整数和。函数名和默认参数值不同，但这些是表面差异，不影响逻辑等价性。 [向量相似度: 0.00] |
| 198 | 205 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都实现了计算从1到n的整数和的功能，仅函数名和默认参数值不同，但这些是表面差异。 [向量相似度: 0.00] |
| 198 | 206 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是通过循环累加从1到n的整数（x**1等价于x）来计算总和。唯一的区别是函数名和默认参数值（49 vs 52），但这些是表面差异，不影响核心功能。 [向量相似度: 0.00] |
| 198 | 207 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是通过循环累加从1到n的整数（x**1等价于x）来计算总和。唯一的区别是函数名和默认参数值（49 vs 23），但这些是表面差异，不影响核心功能实现。 [向量相似度: 0.00] |
| 198 | 208 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是通过循环累加从1到n的整数之和，仅函数名和默认参数值不同，但这些是表面差异。 [向量相似度: 0.00] |
| 198 | 209 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是计算从1到n的整数累加和。仅函数名和默认参数值不同，但这些是表面差异，不影响核心功能。 [向量相似度: 0.00] |
| 198 | 210 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是通过循环累加从1到n的整数（x**1等价于x）来计算总和。唯一的区别是函数名和默认参数值（49 vs 55），但这些是表面差异，不影响核心功能。 [向量相似度: 0.00] |
| 198 | 211 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和算法意图上完全相同，都是通过循环累加从1到n的整数（x**1等价于x）来计算总和。函数名和默认参数值不同属于表面差异，不影响核心功能实现。 [向量相似度: 0.00] |
| 198 | 212 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是计算从1到n的整数和。唯一的区别是函数名和默认参数值，但这些是表面差异，不影响核心功能。 [向量相似度: 0.00] |
| 198 | 213 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是计算从1到n的整数之和。唯一的区别是函数名和默认参数值，但这些是表面差异，不影响核心功能。 [向量相似度: 0.00] |
| 198 | 214 | 100 | hybrid_pipeline | 两段代码都实现了从1到n的整数求和算法，逻辑流程、数据结构和关键算法完全相同，仅函数名和默认参数值不同，但这些是表面差异。 [向量相似度: 0.00] |
| 198 | 215 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是通过循环累加从1到n的整数之和。唯一区别是函数名和默认参数值，但这些不影响核心算法逻辑。 [向量相似度: 0.00] |
| 198 | 216 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都是通过循环遍历从1到n的整数，累加每个整数的1次方（即整数本身），最终返回累加和。仅函数名和默认值不同，这些是表面差异，不影响核心逻辑。 [向量相似度: 0.00] |
| 198 | 217 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都实现了计算从1到n的整数和的功能。差异仅在于函数名和默认参数值，这些是表面差异，不影响核心逻辑。 [向量相似度: 0.00] |
| 198 | 218 | 100 | hybrid_pipeline | 两段代码在逻辑流程、数据结构和关键算法上完全相同，都实现了计算1到n整数累加和的功能，仅函数名和默认值不同，但这些是表面差异。 [向量相似度: 0.00] |
